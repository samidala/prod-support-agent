# prod-support-agent

It is simple POC built to diagnise and analyse production issues by looking at logs and runbooks and other knowledge bases

EXPORT/set environment variables:
OPENAI_API_KEY=your_openai_api_key

run qapplication 
     mvn spring-boot:run

access the webapp at http://localhost:8081